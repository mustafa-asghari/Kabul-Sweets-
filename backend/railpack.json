{
  "build": {
    "cmd": "pip install --no-cache-dir ."
  },
  "start": {
    "cmd": "uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 4 --loop uvloop --http httptools --proxy-headers --forwarded-allow-ips *"
  }
}
